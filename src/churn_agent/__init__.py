"""Agente de retención anti-churn con human-in-the-loop.

Paquete núcleo reutilizable. La frontera núcleo/dominio se mantiene:
los subpaquetes (`core`, `data`, `model`, `economics`, `agent`, `api`)
dependen de `core`, nunca al revés.
"""

__version__ = "0.1.0"
